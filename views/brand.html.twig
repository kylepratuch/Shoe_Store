<html>
<head>
    <title>{{ brand.getBrandName }}</title>
</head>
<body>
    <h1>{{ brand.getBrandName }}</h1>

    <h3>{{ brand.getBrandName }} shoes are carried in these stores:</h3>
        {% if stores is not empty %}
            <ul>
                {% for store in stores %}
                    <li>{{ store.getStoreName }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>No stores yet!</p>
        {% endif %}

    <h4>Add a store to your list of carriers:</h4>

    <form action='/add_stores' method='post'>
        <input id='brand_id' name='brand_id' type='hidden' value='{{ brand.getId }}'>
        <label for='store_id' name='store_id' type='text'>Select a store:</label>
        <select id='store_id' name='store_id' type='text'>
            {% for store in all_stores %}
                <option value='{{ store.getId }}'> {{ store.getStoreName }} </option>
            {% endfor %}
        </select>
        <button type='submit'>Add</button>
    </form>

    <p><a href='/'>Home</a></p>

</body>
</html>
